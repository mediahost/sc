<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<title>{ifset #title}{include title|striptags} | {/ifset}{_'Source-Code'}</title>
	</head>

	<body>

		<div n:foreach="$flashes as $flash" class="flash {$flash->type}">{$flash->message}</div>

		{include content}

	</body>
</html>
